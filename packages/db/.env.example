# Postgres connection string used by the app and by drizzle-kit
DATABASE_URL=postgres://postgres:postgres@localhost:5432/midnightmunches

# Optional: postgres.js pool size (default 10)
DATABASE_POOL_MAX=10

# Optional: set to "true" to require TLS on the connection
DATABASE_SSL=false
